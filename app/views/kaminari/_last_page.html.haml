- url = url.split('?').first
- if current_page.last?
  %li.disabled
    = link_to raw(t 'views.pagination.last'), url, :onclick => 'javascript:return false;'
- else
  %li
    = link_to raw(t 'views.pagination.last'), url, :remote => remote