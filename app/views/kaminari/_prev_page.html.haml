- url = url.split('?').first
- if current_page.first?
  %li.disabled
    = link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :onclick => 'javascript:return false;'
- else
  %li
    = link_to raw(t 'views.pagination.previous'), url, :rel => 'prev', :remote => remote