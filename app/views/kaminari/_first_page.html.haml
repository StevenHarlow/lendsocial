- url = url.split('?').first
- if current_page.first?
  %li.disabled
    = link_to raw(t 'views.pagination.first'), url, :onclick => 'javascript:return false;'
- else
  %li
    = link_to raw(t 'views.pagination.first'), url, :remote => remote