- url = url.split('?').first
- if page.current?
  %li.active
    = link_to page, url, {:rel => page.next? ? 'next' : page.prev? ? 'prev' : nil, :onclick => 'javascript:return false;'}
- else
  %li
    = link_to page, url, {:remote => remote, :rel => page.next? ? 'next' : page.prev? ? 'prev' : nil}